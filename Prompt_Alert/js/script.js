/*var userInput = prompt("Enter your year of birth");
console.log(userInput);
*/
var width = prompt("What is the width of the rectangle?"); //asks for width
var height = prompt("Enter the height");//asks for heightt

var area = width * height; //calculates
console.log(area); //prints it out

var result = "the area of your rectangle is" + " " +area + " sq ft.";
alert(result);